# Copy to .env and fill in. Loaded by dev_env.sh / run.sh.

# Google Gemini API key, used by generate_cartoon.py for image generation.
# Get one at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Optional: override where project data is stored (default: ./data)
# CARTOON_DATA_DIR=
